class N()
{
	int s1;

	N(int s2)
	{
		this.s1  = s2;
	}

	int setAnnotation(int s2)
	{
		return memcpy(s2 + 4, this.s1, strlen(this.s1))
	}
}

int main()
{
	if (argv[] == 1)
		exit(1);
	N *a, *b;
	a = new N(108);
	b = new(108);
	setAnnotation();
	edx();
}
