class N()
{
public:
	char* str;

	int N(char* str)
	{
		this->str = str;
	}

	int setAnnotation(void *s1)
	{
		len = strlen(s1);
		memcpy(this->str, s1, len)
	}

	int operator+(N *n)
	{
		return &(this->str) + n;
	}

	int operator-(N *n, void *arg2)
	{
		return this->val - n->val;
	}
}

int main()
{
	if (argv[] == 1)
		exit(1);

	F *a, *b;
	a = new N(6);
	b = new N(6)
	a->setAnnotation(b);
	edx();
	return **a(a, b);
}
