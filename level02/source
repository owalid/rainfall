void	addr(char *buf)
{
	puts(buf);
	return strdup(buf);
}

void 	p()
{
	char buf[75];

	fflush();
	gets(buf);
	if (!buf)
	{
		printf("(%p)\n");
		exit(1);
	}
}


void	main(int arg, char **argv)
{
	p()
}
